terraform {
  required_version = ">= 1.5.0" # Enforces the minimum Terraform CLI version

  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 5.0" # Constrains the AzureRM provider version
    }
  }
}
provider "azurerm" {
    features {}
}


















































































































































































